#include <stdio.h>
#include <stdlib.h>

//backtracking function
int recordedShows(vector nextShows){
	forall nextshows
		result = recordedshows(nextshows[i])
		if result > actual.result
			actual.result == result
}

int main (int argc, char* argv[]) {

	printf("Enter schedule: ");
	//read schedule into vector/map
	//order by starting time
	//backtracking:
	//	-until nextShows empty
	//		-pick one possible show (start time next >= end time actual)
	//	-return shows recorded
	return 0;
}
